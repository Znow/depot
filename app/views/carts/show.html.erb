<h2>Your Pragmatic Cart</h2>
<ul>
	<% @cart.line_items.each do |item| %> <!-- find @cart's line_items and loop them as item -->
		<li><%= item.quantity %> &times; <%= item.product.title %></li>
	<% end %>
</ul>
